from myenv import TrackingEnv

if __name__ == '__main__':
    # env = TrackingEnv()
    # env.seed(0)
    # newState = env.resetSpecificCurve(1, curveType = 'RandomTest')
    # print(env.randomPhi[0])
    # env = TrackingEnv(3)
    # env.seed(0)
    # newState = env.resetSpecificCurve(1, curveType = 'RandomTest')
    # print(env.randomPhi[0])

    a = [0, 1, 2, 3, 4]
    print(a[2:-3])

    # env.randomTestReset()
